export async function processDaily() {
  console.log(
    "processDaily is disabled on the client. Use the Vercel cron job."
  );
}

export async function pushNews() {
  console.log("pushNews is disabled on the client. Use the Vercel cron job.");
}
